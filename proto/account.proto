syntax = "proto3";

package account.v1;

service AccountService {
    rpc GetAccount(GetAccountRequestDto) returns (GetAccountResponseDto);
}

message GetAccountRequestDto {
    string id = 1;
}

message GetAccountResponseDto {
    string id = 1;
    string phone = 2;
    string email = 3;
    bool is_phone_verified = 4;
    bool is_email_verified = 5;
    Role role = 6;
}

enum Role {
    USER = 0;
    ADMIN = 1;
}
