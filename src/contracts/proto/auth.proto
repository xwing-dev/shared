syntax = "proto3";

package auth.v1;

service AuthService {
    rpc SendOtp(SendOtpDto) returns (SendOtpResponseDto);
}

message SendOtpDto {
    string identifier = 1;
    string type = 2;
}

message SendOtpResponseDto {
    string message = 1;
}